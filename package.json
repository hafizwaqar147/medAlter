{
  "name": "medalter",
  "version": "1.0.0",
  "description": "MedAlter — AI-Driven Alternative Medicine & Holistic Healing Landing Page",
  "scripts": {
    "dev": "concurrently \"npm run tailwind:watch\" \"npm run ts:watch\" \"npm run serve\"",
    "build": "npm run tailwind:build && npm run ts:build",
    "tailwind:watch": "tailwindcss -i ./src/styles/tailwind.css -o ./dist/styles.css --watch",
    "tailwind:build": "tailwindcss -i ./src/styles/tailwind.css -o ./dist/styles.css --minify",
    "ts:watch": "tsc -w",
    "ts:build": "tsc",
    "serve": "live-server dist"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "live-server": "^1.2.2",
    "tailwindcss": "^3.4.0",
    "typescript": "^5.7.0"
  }
}
